<<<<<<< HEAD
﻿@model BoticaMVC.ViewModels.MedicamentosIndexVM
=======
﻿@model IEnumerable<BoticaMVC.Models.Medicamento>
>>>>>>> origin/main

@{
    ViewData["Title"] = "Medicamentos";

    var q = ViewBag.Q as string ?? "";
    var orden = ViewBag.Orden as string ?? "nombre";
    var soloActivos = ViewBag.SoloActivos is bool b ? b : true;

    var hoy = DateTime.Today;
    var limite30 = hoy.AddDays(30);
}

<<<<<<< HEAD
<h2 class="mb-3">Medicamentos (Inventario)</h2>

@if (TempData["ok"] != null)
{
    <div class="alert alert-success alert-dismissible fade show" role="alert">
        @TempData["ok"]
        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
    </div>
}

<form method="get" asp-action="Index" class="row g-2 mb-4">

    <div class="col-md-5">
        <input class="form-control"
               name="q"
               value="@ViewBag.Q"
               placeholder="Buscar por nombre, código, lote o laboratorio..." />
    </div>

    <div class="col-md-2">
        <select class="form-select" name="orden">
            <option value="codigo" selected="@(ViewBag.Orden == "codigo")">Código</option>
            <option value="nombre" selected="@(ViewBag.Orden == "nombre")">Nombre</option>
            <option value="stock" selected="@(ViewBag.Orden == "stock")">Stock</option>
            <option value="vence" selected="@(ViewBag.Orden == "vence")">Vencimiento</option>
        </select>
    </div>

    <div class="col-md-2 d-flex align-items-center">
        <input type="hidden" name="soloActivos" value="false" />
        <input type="checkbox"
               class="form-check-input me-2"
               id="soloActivos"
               name="soloActivos"
               value="true"
               @(ViewBag.SoloActivos ? "checked" : "") />
        <label class="form-check-label" for="soloActivos">Solo activos</label>
    </div>

    <div class="col-md-3 text-end">
        <button type="submit" class="btn btn-primary">Buscar</button>
        <a class="btn btn-success" asp-action="Create">+ Nuevo</a>
    </div>

    <div class="col-12 text-end mt-2">
        <a class="btn btn-outline-dark" asp-action="Reporte">Reporte</a>
        <a class="btn btn-outline-success" asp-action="ReporteExcel">Excel</a>
        <a class="btn btn-outline-danger" asp-action="ReportePdf">PDF</a>
=======
<div class="d-flex align-items-center justify-content-between flex-wrap gap-2 mb-3">
    <div>
        <h1 class="h3 mb-0">Medicamentos</h1>
        <div class="text-muted">Gestión del inventario (búsqueda, orden y acciones)</div>
    </div>

    <div class="d-flex gap-2 flex-wrap">
        <a class="btn app-btn app-btn-success" asp-action="Create">Nuevo</a>
        <a class="btn app-btn app-btn-outline" asp-action="Reporte">Reporte (Imprimir)</a>
        <a class="btn app-btn app-btn-outline" asp-action="ReporteExcel">Excel</a>
        <a class="btn app-btn app-btn-outline" asp-action="ReportePdf">PDF</a>
    </div>
</div>

@if (TempData["ok"] is string okMsg)
{
    <div class="alert alert-success alert-dismissible fade show" role="alert">
        @okMsg
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Cerrar"></button>
    </div>
}

<form method="get" class="row g-2 align-items-end mb-3">
    <div class="col-12 col-md-6 col-lg-4">
        <label class="form-label">Buscar</label>
        <input name="q" value="@q" class="form-control" placeholder="Nombre / Código / Lote / Laboratorio" />
    </div>

    <div class="col-12 col-md-3 col-lg-2">
        <label class="form-label">Orden</label>
        <select name="orden" class="form-select">
            <option value="nombre" selected="@(orden == "nombre")">Nombre</option>
            <option value="codigo" selected="@(orden == "codigo")">Código</option>
            <option value="stock" selected="@(orden == "stock")">Stock</option>
            <option value="vence" selected="@(orden == "vence")">Vencimiento</option>
        </select>
    </div>

    <div class="col-12 col-md-3 col-lg-2">
        <input type="hidden" name="soloActivos" value="false" />
        <div class="form-check mt-4">
            <input class="form-check-input" type="checkbox" id="soloActivos" name="soloActivos" value="true" @(soloActivos ? "checked" : "") />
            <label class="form-check-label" for="soloActivos">Solo activos</label>
        </div>
    </div>

    <div class="col-12 col-md-6 col-lg-2 d-grid">
        <button type="submit" class="btn app-btn app-btn-primary">Aplicar</button>
    </div>

    <div class="col-12 col-md-6 col-lg-2 d-grid">
        <a class="btn app-btn app-btn-outline" asp-action="Index">Limpiar</a>
>>>>>>> origin/main
    </div>
</form>

<div class="table-responsive">
<<<<<<< HEAD
    <table class="table table-bordered table-striped align-middle">
        <thead class="table-light">
            <tr>
                <th>Estado</th>
                <th>Código</th>
                <th>Nombre</th>
                <th>Lab</th>
                <th>Vence</th>
                <th>Stock</th>
                <th>Venta</th>
                <th>Receta</th>
                <th class="text-center">Acciones</th>
            </tr>
        </thead>
        <tbody>
            @if (Model.Items != null && Model.Items.Any())
            {
                @foreach (var m in Model.Items)
                {
                    var vencido = m.FechaVencimiento < hoy;
                    var porVencer = !vencido && m.FechaVencimiento <= hoy.AddDays(30);
                    var stockBajo = m.Stock <= m.StockMinimo;

                    <tr>
                        <td>
                            @if (!m.Activo)
                            {
                                <span class="badge bg-secondary">Inactivo</span>
                            }
                            else if (vencido)
                            {
                                <span class="badge bg-danger">Vencido</span>
                            }
                            else if (porVencer)
                            {
                                <span class="badge bg-warning text-dark">Por vencer</span>
                            }
                            else if (stockBajo)
                            {
                                <span class="badge bg-info text-dark">Stock bajo</span>
                            }
                            else
                            {
                                <span class="badge bg-success">OK</span>
                            }
                        </td>
                        <td>@m.Codigo</td>
                        <td>@m.Nombre</td>
                        <td>@m.Laboratorio</td>
                        <td>@m.FechaVencimiento.ToString("dd/MM/yyyy")</td>
                        <td>@m.Stock / Min: @m.StockMinimo</td>
                        <td>S/ @m.PrecioVenta.ToString("0.00")</td>
                        <td>@(m.RequiereReceta ? "Sí" : "No")</td>
                        <td class="text-center">
                            <a class="btn btn-sm btn-outline-primary" asp-action="Edit" asp-route-id="@m.Id">Editar</a>
                            <a class="btn btn-sm btn-outline-secondary" asp-action="Details" asp-route-id="@m.Id">Ver</a>
                            <a class="btn btn-sm btn-outline-danger" asp-action="Delete" asp-route-id="@m.Id">Desactivar</a>
=======
    <table class="table table-hover align-middle">
        <thead class="table-light">
            <tr>
                <th>Código</th>
                <th>Nombre</th>
                <th>Laboratorio</th>
                <th>Vence</th>
                <th class="text-end">Stock</th>
                <th class="text-end">Min</th>
                <th class="text-end">Venta</th>
                <th class="text-end">Acciones</th>
            </tr>
        </thead>

        <tbody>
            @if (Model != null && Model.Any())
            {
                foreach (var m in Model)
                {
                    var stockBajo = m.Stock <= m.StockMinimo;
                    var vencido = m.FechaVencimiento < hoy;
                    var porVencer = !vencido && m.FechaVencimiento <= limite30;

                    var badgeStock = stockBajo ? "bg-warning text-dark" : "bg-success";
                    var badgeVence = vencido ? "bg-danger" : (porVencer ? "bg-warning text-dark" : "bg-success");

                    <tr>
                        <td class="fw-semibold">@m.Codigo</td>
                        <td>@m.Nombre</td>
                        <td>@m.Laboratorio</td>
                        <td>
                            <span class="badge @badgeVence">
                                @m.FechaVencimiento.ToString("dd/MM/yyyy")
                            </span>
                        </td>
                        <td class="text-end">
                            <span class="badge @badgeStock">@m.Stock</span>
                        </td>
                        <td class="text-end">@m.StockMinimo</td>
                        <td class="text-end">@m.PrecioVenta.ToString("0.00")</td>
                        <td class="text-end">
                            <a class="btn btn-sm btn-outline-secondary" asp-action="Details" asp-route-id="@m.Id">Ver</a>
                            <a class="btn btn-sm btn-outline-primary" asp-action="Edit" asp-route-id="@m.Id">Editar</a>
                            <a class="btn btn-sm btn-outline-danger" asp-action="Delete" asp-route-id="@m.Id">Eliminar</a>
>>>>>>> origin/main
                        </td>
                    </tr>
                }
            }
            else
            {
                <tr>
<<<<<<< HEAD
                    <td colspan="9" class="text-center text-muted py-4">
                        No se encontraron medicamentos.
=======
                    <td colspan="8" class="text-center text-muted py-4">
                        No se encontraron medicamentos con los filtros actuales.
>>>>>>> origin/main
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>
<<<<<<< HEAD

@if (ViewBag.TotalPaginas > 1)
{
    <nav class="mt-4 d-flex justify-content-center">
        <ul class="pagination botica-pagination">

            <li class="page-item @(ViewBag.PaginaActual == 1 ? "disabled" : "")">
                <a class="page-link"
                   asp-action="Index"
                   asp-route-q="@ViewBag.Q"
                   asp-route-orden="@ViewBag.Orden"
                   asp-route-soloActivos="@ViewBag.SoloActivos"
                   asp-route-pagina="@(ViewBag.PaginaActual - 1)">
                    Anterior
                </a>
            </li>

            @for (int i = 1; i <= (int)ViewBag.TotalPaginas; i++)
            {
                <li class="page-item @(i == (int)ViewBag.PaginaActual ? "active" : "")">
                    <a class="page-link"
                       asp-action="Index"
                       asp-route-q="@ViewBag.Q"
                       asp-route-orden="@ViewBag.Orden"
                       asp-route-soloActivos="@ViewBag.SoloActivos"
                       asp-route-pagina="@i">
                        @i
                    </a>
                </li>
            }

            <li class="page-item @(ViewBag.PaginaActual == ViewBag.TotalPaginas ? "disabled" : "")">
                <a class="page-link"
                   asp-action="Index"
                   asp-route-q="@ViewBag.Q"
                   asp-route-orden="@ViewBag.Orden"
                   asp-route-soloActivos="@ViewBag.SoloActivos"
                   asp-route-pagina="@(ViewBag.PaginaActual + 1)">
                    Siguiente
                </a>
            </li>

        </ul>
    </nav>
}
=======
>>>>>>> origin/main
